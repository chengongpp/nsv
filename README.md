# Noindex Serving

No-index HTTP file server for current directory, aborts when you try to serve `/` and `$HOME`.

> [!WARNING]
> Vibe coded project, grab only if you are brave af like Tachibana Sherry.

## Usage

```bash
./nsv
./nsv 8001
# don't do this
./nsv --force
```
